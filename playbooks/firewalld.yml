---
- name: Setup FirewallD
  hosts: localhost
  connection: local
  become: true
  tasks: 
    - name: Allow KDE Connect traffic
      ansible.posix.firewalld:
        service: kdeconnect
        zone: public
        permanent: yes
        state: enabled 
        immediate: true
